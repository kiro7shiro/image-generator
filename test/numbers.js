const { Arrays } = require('../src/Arrays.js')
const { Trainer } = require('../src/Trainer.js')

/* describe('Numbers', function () {
    it('distribution', function () {
        const array = Trainer.activations
        const left = array.length - 1 / 2
        const part = left / 3
        const weights = [left, part, part, part]
        const dist = Arrays.createDistribution(array, weights, 10)
        console.log({ weights, dist })
    })
}) */